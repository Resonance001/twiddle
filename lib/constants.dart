const rotateDuration = Duration(milliseconds: 300);

class Offset{
  Offset._();

  static const topBarHeight = 70.0;
  static const topBarOffset = 10.0;

  static const bottomGameOffset = 100.0;
  static const topGameOffset = bottomGameOffset + topBarHeight;
}